# MediaMTX configuration file
paths:
  input:
    source: rtsp://127.0.0.1:8554/video_stream_ball
    sourceProtocol: tcp

# RTSP settings
rtspAddress: :8554

# WebRTC settings
webrtcAddress: :8889
webrtcICEServers: ["stun:stun.l.google.com:19302"]

# Disable unused features
api: false
metrics: false
pprof: false

#logLevel: debug