# MediaMTX configuration file
paths:
  all:
    readUser: admin
    readPass: password
    
  input:
    source: ${TARGET_RTSP_URL}
    sourceProtocol: tcp
    
protocols: [tcp, udp]

rtmp:
  enabled: false

rtsp:
  enabled: true

logLevel: info